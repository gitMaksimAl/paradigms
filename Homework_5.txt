% Написать программу на языке Prolog для вычисления суммы элементов списка. На вход подается целочисленный
% массив. На выходе - сумма.
sum([], 0).
sum([H], H).
sum([H|T], S) :-
    sum(T, S1), S is H + S1.
